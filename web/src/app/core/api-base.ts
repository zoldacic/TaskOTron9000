// Base URL for the backend API.
// Empty in dev — requests to /api/* are proxied to the backend by proxy.conf.json
// (ng serve --proxy-config proxy.conf.json), so no CORS is involved.
// For a separately-hosted production build, set this to the API origin.
export const API_BASE = '';
